title = "Mahiva Gitleaks configuration"

[[rules]]
id = "generic-api-key"
regex = '''(?i)(api[_-]?key|token|secret)["'\s:=]+[A-Za-z0-9_\-]{8,}'''

[allowlist]
paths = [
  "node_modules/**",
  "dist/**",
  "coverage/**",
  ".mahiva/**",
]
